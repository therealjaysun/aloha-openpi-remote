# S0827.02 — Two-arm Push-PI glyph

- **Objective:** Evaluate the same rigid Greek π task with both ALOHA arms available; record whether the policy actually uses one or both.
- **Inputs/prerequisites:** Shared contracts in [`00-overview.md`](00-overview.md); passing S0827.01 environment/reset tests; Phase 04 buffered runner; selected policy profile and explicit scenario prompt.
- **Implementation tasks:** Select the same rigid-π descriptor and RNG stream as S0827.01; pass both arms and both grippers through unchanged; send one fixed both-arms prompt containing the three ordered orient, approach, and incremental-push instructions; keep scene hash, sampled/settled pose, target, camera, success rules, episode length, and seeds fixed; record per-arm travel/contact/participation/interference without making them success requirements.
- **Files expected to change:** Only the shared scenario prompt, frozen prompt/hash test, and documentation; do not fork geometry, reset, reward, display, video, runner, or action code from S0827.01.
- **Validation:** Assert paired seeds produce identical scene hashes and sampled/settled/target π poses; verify the complete finite 14-D action reaches the simulator unchanged; test order-independent named contacts and participation; keep historical matrix evidence labeled; run one exact seed-0 6,000-step fixed-prompt diagnostic for each current π₀.₅ checkpoint; inspect both three-view MP4s, both plots, telemetry, GPU evidence, and cleanup.
- **Acceptance:** Each current checkpoint comparison reaches its exact 6,000-step `time_limit`; success, fall, and off-table events remain recorded without ending the run. Partial runs do not count and must be rerun. Both arm command groups are available, artifacts are valid, paired manifests prove identical scene initial conditions to S0827.01, and results report actual per-arm participation plus per-profile success/time/raw-error deltas without treating one seed as a statistical claim.
- **Planned commit:** Included in `feat(sim): add shared Push-PI environment` and `test(scenarios): validate Push-PI matrix workflow`.
- **Actual findings:** The installed MuJoCo/Gym-Aloha model is already bimanual and its policy/state contract already matches both model profiles.
- **Actual validation:** The six historical locked-action episodes remain unchanged. Exact unlocked candidate `42a9e10` passed 427 tests + 1 platform skip, lint/format/Bash, secret/public gates, exact PC sync, and finite three-camera smoke for both profiles. Each profile then completed exact steps 0–5,999 with one prompt stage, zero transitions, 6,000 finite 14-D action/trajectory rows, 6,000 `224×672` three-view frames, 0% coverage, and no named contact/lift/fall/off-table. π₀.₅ base used `76.0704/232.1311 rad` left/right travel and worsened target error by `0.00274457 m`; π₀ simulation-checkpoint used `9.7060/32.7359 rad` and changed error by only `-0.00000318 m`. Both 14+14 series plots and five-timepoint triptychs passed inspection; cleanup left the port free.
- **Current comparison:** Candidate `196f8e5` completed exact 6,000-step/120-second π₀.₅ base and Trossen runs. Base recorded no outcome event in 198.642 wall seconds; Trossen recorded right contact, lift, and fall in 198.637 wall seconds. Both stayed at 0% coverage and passed exact video/trajectory checks.
- **Remaining blockers:** None for the requested paired diagnostics. Unlocked four-scenario matrix acceptance was not requested and remains unclaimed.
- **Completion status:** Current π₀.₅ checkpoint comparison complete; historical evidence remains separately labeled.
