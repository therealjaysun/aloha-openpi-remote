# 05.04 — Performance summary

- **Objective:** Produce an honest, compact infrastructure and task report for each policy profile.
- **Inputs/prerequisites:** Complete per-profile runs plus GPU/local telemetry.
- **Implementation tasks:** Retain existing metrics and add trajectory sample/joint counts, exact applied-step coverage, plot status, and safe local plot IDs. After each complete or partial episode, atomically render one PNG from valid step rows with elapsed time on X and fixed-range percent on Y; overlay 14 solid actual series grouped blue/orange by arm, thinner dashed commands, an external legend, and a dotted 50% reference labelled as zero only for symmetric waist/forearm-roll/wrist-rotate joints. Do not draw one universal home line because home percentage differs by joint. Grippers use the simulator's documented normalized coordinate; note that its XML stops lie slightly inside the nominal close/open calibration.
- **Files expected to change:** Telemetry aggregator/test fixtures, `README.md` status/results, plan actual-results fields; generated summaries ignored unless deliberately sanitized as tiny fixtures.
- **Validation:** Known fixed-limit normalization; synthetic full/partial plot generation and atomic-replace failure; inspect each exact-candidate plot; assert 14 actual series and, when present, 14 command series; cross-check samples against applied steps; ensure summaries contain no path or machine identifier.
- **Acceptance:** Existing comparison remains intact; every episode with valid trajectory rows has a readable atomic plot, full coverage for passing runs, and only safe compact trajectory metadata in publishable summaries.
- **Planned commit:** `docs(perf): summarize remote ALOHA runs`.
- **Actual findings:** Candidate `de63e19` completed three episodes per profile. π₀: infrastructure/task 3/3, 731 steps, 38 requests, warmed inference mean/p95/max 327.28/392.83/448.95 ms, active-rate mean/p95 48.21/48.24 Hz, and GPU memory/utilization max 15,403 MiB/46%. Experimental π₀.₅: infrastructure 3/3, task 0/3, 900 steps, 40 requests, warmed inference mean/p95/max 418.27/515.54/672.15 ms, active-rate mean/p95 47.56/48.13 Hz, and GPU memory/utilization max 15,859 MiB/39%. Neither profile supports an uninterrupted 50 Hz claim.
- **Remaining blockers:** Amended exact-candidate profile runs and six-plot inspection.
- **Completion status:** Amendment in progress; the prior ignored summaries remain baseline evidence only.
