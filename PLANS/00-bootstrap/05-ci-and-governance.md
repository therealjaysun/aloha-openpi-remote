# 00.05 — CI and governance

- **Objective:** Add public-repo safeguards and CPU-only checks without weakening upstream quality gates.
- **Inputs/prerequisites:** Baseline repo; implementation test commands finalized.
- **Implementation tasks:** Add SECURITY/CONTRIBUTING/project PR template and delete derivative-inapplicable CODEOWNERS; create exact-version/hashes lightweight lock `requirements/project-test.txt` and an ignored Python 3.10 `examples/aloha_sim/.venv` containing only those pure-test/lint dependencies in phase 00; make `make test` invoke that venv's `python -m pytest tests` and add root `tests` to `pyproject.toml` discovery. Phase 01 reuses the same venv, syncs the official sim lock, then reinstalls the project-test lock so pytest/Ruff remain. Public CI installs the same lock plus only lightweight project/client dependencies as tests land; it never installs the root model stack. Integrate the already-working secret scan into `ci.yml`; add hosted Ubuntu pure tests, Ruff, shell syntax, and public-output scan; pin Actions to immutable SHAs, set workflow/repository permissions to read-only, fetch sufficient history for scanning, and never use `pull_request_target`; retain upstream test commands as documented WSL/manual gates, but explicitly guard the private-runner job to the official upstream repository so it is skipped—not passed or required—in the derivative. Push the hardened phase branch while repository Actions are disabled, inspect the remote workflow diff, set default workflow permissions to read-only, then enable Actions and open PR 1 so the first execution uses hardened files. Branch protection requires only runnable public checks.
- **Files expected to change:** `requirements/project-test.txt`, `pyproject.toml`, `SECURITY.md`, `CONTRIBUTING.md`, `.github/CODEOWNERS` deletion, `.github/pull_request_template.md`, `.github/workflows/{ci,pre-commit,test}.yml`, `scripts/secret_scan.sh`, `Makefile`, `.gitignore`.
- **Validation:** `make test`; `make lint`; `bash -n scripts/*.sh`; `make secret-scan`; inspect local and remote workflow YAML/permissions; verify no workflow ran on the baseline push; enable Actions only after the hardened branch is remote; open PR 1 and verify runnable public checks.
- **Acceptance:** Ordinary PR CI needs no GPU/PC/secrets and imports no simulator/model stack for pure tests; existing test coverage is not weakened; unavailable upstream private-runner checks are labeled unavailable, never passed; vulnerability reporting and contribution scope are clear; no guessed reviewers.
- **Planned commits:** `docs: add project governance`; `ci: add lightweight public checks`.
- **Actual findings:** Upstream test workflow requires `openpi-verylarge`, which a public derivative cannot assume. `gitleaks`, ShellCheck, and FFmpeg are not currently installed locally; Ruff and pytest executables exist outside the planned project venv.
- **Remaining blockers:** Install the selected secret scanner during implementation; `make secret-scan` must fail closed when it is unavailable. GitHub Actions cannot run until origin exists.
- **Completion status:** Planned.
